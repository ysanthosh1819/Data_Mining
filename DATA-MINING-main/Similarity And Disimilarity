import numpy as np
from scipy.spatial.distance import euclidean, cosine

x = np.array([1, 2, 3])
y = np.array([4, 5, 6])

print("Mean:", np.mean(x))
print("Std Dev:", np.std(x))

print("Euclidean Distance:", euclidean(x, y))
print("Cosine Similarity:", 1 - cosine(x, y))
